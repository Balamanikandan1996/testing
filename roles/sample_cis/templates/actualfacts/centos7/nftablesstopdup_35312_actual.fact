#!/bin/bash

cmd=$(sudo rpm -qa nftables | wc -l)
if [ $(sudo rpm -qa nftables | wc -l) -ne 0 ]; then
   echo "{ \"nftablesstopdup_35312_actual\" : \"\"}"
else
   echo "{ \"nftablesstopdup_35312_actual\" : \"$cmd\"}"
   exit 0
fi

