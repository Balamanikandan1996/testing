#!/bin/bash
cmd=$(sudo iptables -L | grep "policy DROP" | paste -sd "," -)
if [ $(sudo iptables -L | grep "policy DROP" | wc -l) -eq 0 ]; then
   echo "{ \"firewallpolicyip_35321_actual\" : \"\" }"
else
   echo "{ \"firewallpolicyip_35321_actual\" : \"${cmd}\" }"
   exit 0
fi

