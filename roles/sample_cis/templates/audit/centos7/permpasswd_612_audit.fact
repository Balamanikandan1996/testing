#!/bin/bash
# ** AUTO GENERATED **

# 6.1.2 - Ensure permissions on /etc/passwd are configured

if [ "$(stat /etc/passwd | grep 0644 | grep ".*Uid:.*0/.*root" | grep ".*Gid.*0/.*root" | wc -l)" -eq 0 ]; then

   echo "{ \"6.1.2 - Ensure permissions on /etc/passwd are configured\" : \"FAILED\"}"
else
   echo "{ \"permpasswd_612_audit\" : \"PASSED\"}"
   exit 0
fi


