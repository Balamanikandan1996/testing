#!/bin/bash
# ** AUTO GENERATED **

# 4.1.2.1 - Ensure audit log storage size is configured

if [ $(sudo grep "^max_log_file.*8" /etc/audit/auditd.conf 2> /dev/null | wc -l) -eq 0 ]; then
   echo "{\"4.1.2.1 - Ensure audit log storage size is configured\":\"FAILED\"}"
else
   echo "{\"auditlogsize_4121_audit\":\"PASSED\"}"
   exit 1
fi

