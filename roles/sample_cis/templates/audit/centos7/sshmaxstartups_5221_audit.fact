#!/bin/bash
# ** AUTO GENERATED **

# 5.2.21 - Ensure SSH MaxStartups is configured

if [ $(sudo grep "MaxStartups 10:30:60" /etc/ssh/sshd_config | wc -l) -eq 0 ]; then
   echo "{\"5.2.21 - Ensure SSH MaxStartups is configured\":\"FAILED\"}"
else
   echo "{\"sshmaxstartups_5221_audit\":\"PASSED\"}"
   exit 1
fi

