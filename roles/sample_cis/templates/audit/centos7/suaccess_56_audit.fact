#!/bin/bash
# ** AUTO GENERATED **

# 5.6 - Ensure access to the su command is restricted 

if [ $(sudo grep -E '^\s*auth\s+required\s+pam_wheel\.so\s+(\S+\s+)*use_uid\s+(\S+\s+)*group=\S+\s*(\S+\s*)*(\s+#.*)?$' /etc/pam.d/su | wc -l) -eq 0 ]; then
   echo "{ \"5.6 - Ensure access to the su command is restricted \" : \"FAILED\"}"
else
   echo "{ \"suaccess_56_audit\" : \"PASSED\"}"
   exit 0
fi
