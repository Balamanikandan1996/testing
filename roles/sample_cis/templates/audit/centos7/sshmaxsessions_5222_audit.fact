#!/bin/bash
# ** AUTO GENERATED **

# 5.2.22 - Ensure SSH MaxSessions is limited

if [ $(sudo grep "^MaxSessions.*10" /etc/ssh/sshd_config | wc -l) -eq 0 ]; then
   echo "{\"5.2.22 - Ensure SSH MaxSessions is limited\":\"FAILED\"}"
else
   echo "{\"sshmaxsessions_5222_audit\":\"PASSED\"}"
   exit 1
fi

