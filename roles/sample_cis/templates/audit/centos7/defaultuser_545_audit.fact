#!/bin/bash
# ** AUTO GENERATED **

# 5.4.5 Ensure default user umask is configured

if [ $(grep "umask.*027" /etc/bashrc | wc -l) -eq 0 ] || [ $(grep "umask.*027" /etc/profile | wc -l) -eq 0 ]; then
   echo "{\"5.4.5 Ensure default user umask is configured\":\"FAILED\"}"
else
   echo "{\"defaultuser_545_audit\":\"PASSED\"}"
   exit 1
fi

