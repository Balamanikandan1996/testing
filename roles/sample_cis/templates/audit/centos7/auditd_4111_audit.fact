#!/bin/bash
# ** AUTO GENERATED **

# 4.1.1.1 - Ensure auditd is installed

if [ $(sudo rpm -qa | grep audit | wc -l) -eq 0 ] || [ $(sudo rpm -qa | grep audit-libs | wc -l) -eq 0 ]; then
   echo "{ \"4.1.1.1 - Ensure auditd is installed\" : \"FAILED\"}"
else
   echo "{ \"auditd_4111_audit\" : \"PASSED\"}"
   exit 0
fi

