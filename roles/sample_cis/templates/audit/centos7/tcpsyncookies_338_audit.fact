#!/bin/bash
# **UTO GENERATED **

# 3.3.8 - Ensure TCP SYN Cookies is enabled (Scored)

if [ "$(sudo sysctl net.ipv4.tcp_syncookies)" != "net.ipv4.tcp_syncookies = 1" ] || [ $(sudo grep "^net.ipv4.tcp_syncookies=1" /etc/sysctl.conf | wc -l) -eq 0 ]; then
   echo "{\"3.3.8 - TCP SYN Cookies is not enabled\" :\"FAILED\"}"
else
   echo "{\"tcpsyncookies_338_audit\" :\"PASSED\"}"
   exit 1
fi
