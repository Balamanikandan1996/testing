#!/bin/bash
# ** AUTO GENERATED **

# 6.2.2 - Ensure /etc/shadow password fields are not empty

if [ $(awk -F: '$5==90 && $6==7 {print}' /etc/shadow | awk -F: ' $2=="!" || $2=="" || $2=="!!"{print $1}' | wc -l) -gt 0 ]; then
   echo "{\"6.2.2 - Ensure /etc/shadow password fields are not empty\" : \"FAILED\"}"
else
   echo "{\"emptypasswd_622_audit\" : \"PASSED\"}"
   exit 1
fi


