#!/bin/bash
# ** AUTO GENERATED **

# 1.5.2 - Ensure permissions on bootloader config are configured (Scored)

if [ $(sudo stat /boot/grub2/grub.cfg | grep "0600.*Uid:.*0/.*root.*Gid:.*0/.*root" | wc -l) -eq 0 ] || [ $(sudo stat /boot/grub2/user.cfg | grep "0600.*Uid:.*0/.*root.*Gid:.*0/.*root" | wc -l) -eq 0 ]; then
   echo "{ \"1.5.2 - Permissions on bootloader config are not configured\" : \"FAILED\" }"
else
   echo "{ \"bootloadperm_152_audit\" : \"PASSED\" }"
fi
