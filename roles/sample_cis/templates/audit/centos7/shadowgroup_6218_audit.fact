#!/bin/bash
# ** AUTO GENERATED **

# 6.2.18 - Ensure shadow group is empty

if [ $(sudo grep "^shadow:x:" /etc/group | awk -F: {'print $4'} | grep -v '^\s*$' | wc -l) -ne 0 ]; then
   echo "{ \"6.2.18 - Ensure shadow group is empty\" : \"FAILED\"}"
else
   echo "{ \"shadowgroup_6218_audit\" : \"PASSED\"}"
   exit 0
fi

