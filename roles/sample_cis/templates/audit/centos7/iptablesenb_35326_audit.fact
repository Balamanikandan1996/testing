#!/bin/bash
# ** AUTO GENERATED **

# 3.5.3.2.6 - Ensure iptables is enabled and running

if [ $(systemctl status iptables | grep "Active: active" | wc -l) -eq 1 ]; then
    echo "{ \"iptablesenb_35326_audit\" : \"PASSED\" }"
else
   echo "{ \"3.5.3.2.6 - Ensure iptables is enabled and running\" : \"FAILED\" }"
   exit 0
fi

