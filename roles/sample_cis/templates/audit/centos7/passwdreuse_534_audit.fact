#!/bin/bash
# ** AUTO GENERATED **

# 5.3.4 - Ensure password reuse is limited

if [ $(grep "remember=5" /etc/pam.d/password-auth | wc -l) -eq 0 ] || [ $(grep "remember=5" /etc/pam.d/system-auth | wc -l) -eq 0 ]; then
   echo "{ \"5.3.4 - Ensure password reuse is limited\" : \"FAILED\"}"
else
   echo "{ \"passwdreuse_534_audit\" : \"PASSED\"}"
   exit 1
fi


