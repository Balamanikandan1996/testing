#!/bin/bash
# ** AUTO GENERATED **

# 1.3.1 - Ensure sudo is installed (Scored)

if [ $(sudo rpm -qa | grep "sudo-[0-9]" | wc -l) -eq 0 ]; then
   echo "{ \"1.3.1 - sudo is not installed\" : \"FAILED\" }"
else
   echo "{ \"sudo_131_audit\" : \"PASSED\" }"

fi
