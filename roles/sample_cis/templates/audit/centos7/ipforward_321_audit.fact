#!/bin/bash
# ** AUTO GENERATED **

# 3.2.1 - Ensure IP forwarding is disabled (Scored)

if [ $(sudo sysctl net.ipv4.ip_forward | grep 0 | wc -l) -eq 0 ] || [ $(sudo grep "^net.ipv4.ip_forward=0" /etc/sysctl.conf | wc -l) -eq 0 ]; then
   echo "{ \"3.2.1 - IP forwarding is not disabled\" : \"FAILED\" }"
else
   echo "{ \"ipforward_321_audit\" : \"PASSED\" }"
fi
