#!/bin/bash
# ** AUTO GENERATED **

# 2.2.11 - Ensure HTTP Server is not installed (Scored)

if [ $(sudo rpm -qa httpd | wc -l) -ne 0 ]; then
   echo "{ \"2.2.11 - HTTP server is installed\" : \"FAILED\" }"
else
   echo "{ \"http_2211_audit\" : \"PASSED\" }"
fi
