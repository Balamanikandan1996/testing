#!/bin/bash
# ** AUTO GENERATED **

# 2.2.9 - Ensure DNS Server is not installed (Scored)

if [ $(sudo rpm -qa bind | wc -l) -ne 0 ]; then
   echo "{ \"2.2.9 - DNS server is installed\" : \"FAILED\" }"
else
   echo "{ \"dns_229_audit\" : \"PASSED\" }"
fi
