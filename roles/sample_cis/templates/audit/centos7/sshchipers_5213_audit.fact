#!/bin/bash
# ** AUTO GENERATED **

# 5.2.13 - Ensure only strong Ciphers are used 

if [ $(sudo sshd -T | grep ciphers | wc -l) -eq 0 ]; then
   echo "{ \"5.2.13 - Ensure only strong Ciphers are used\" : \"FAILED\"}"
else
   echo "{ \"sshchipers_5213_audit\" : \"PASSED\"}"
   exit 0
fi

