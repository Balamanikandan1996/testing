#!/bin/bash
# ** AUTO GENERATED **

# 2.2.6 - Ensure LDAP server is not installed (Scored)

if [ $(sudo rpm -qa openldap-servers | wc -l) -ne 0 ]; then
   echo "{ \"2.2.6 - LDAP server is Installed\" : \"FAILED\" }"
else
   echo "{ \"ldap_226_audit\" : \"PASSED\" }"
fi
