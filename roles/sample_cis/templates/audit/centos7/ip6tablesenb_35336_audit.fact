#!/bin/bash
# ** AUTO GENERATED **

# 3.5.3.3.6 - Ensure ip6tables is enabled and running

if [ $(systemctl is-enabled ip6tables  | grep "enabled" | wc -l) -eq 1 ] || [ $(systemctl status ip6tables | grep " Active: active (running) " | wc -l) -eq 1 ]; then
    echo "{ \"ip6tablesenb_35336_audit\" : \"PASSED\" }"
else
   echo "{ \"3.5.3.3.6 - Ensure ip6tables is enabled and running\" : \"FAILED\" }"
   exit 0
fi

