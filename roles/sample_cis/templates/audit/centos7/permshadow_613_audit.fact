#!/bin/bash
# ** AUTO GENERATED **

# 6.1.3 - Ensure permissions on /etc/shadow are configured

if [ "$(stat /etc/shadow | grep 0000 | grep ".*Uid:.*0/.*root" | grep ".*Gid.*0/.*root" | wc -l)" -eq 0 ]; then

   echo "{ \"6.1.3 - Ensure permissions on /etc/shadow are configured\" : \"FAILED\"}"
else
   echo "{ \"permshadow_613_audit\" : \"PASSED\"}"
   exit 0
fi




