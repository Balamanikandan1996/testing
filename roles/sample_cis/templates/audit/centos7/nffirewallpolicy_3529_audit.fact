#!/bin/bash
# ** AUTO GENERATED **

# 3.5.2.9 Ensure default deny firewall policy 

if [ $(sudo nft list ruleset | grep "hook input" | wc -l) -eq 0 ] || [ $(sudo nft list ruleset | grep "hook forward" | wc -l) -eq 0 ] || [ $(sudo nft list ruleset | grep "hook output" | wc -l) -eq 0 ]; then
   echo "{ \"3.5.2.9 Ensure default deny firewall policy\" : \"FAILED\" }"
else
   echo "{ \"nffirewallpolicy_3529_audit\" : \"PASSED\" }"
   exit 1
fi


