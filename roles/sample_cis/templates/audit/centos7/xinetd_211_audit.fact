#!/bin/bash
# ** AUTO GENERATED **

# 2.1.1 - Ensure xinetd is not installed (Scored)

if [ $(sudo rpm -qa | grep xinetd | wc -l) -ne 0 ]; then
   echo "{ \"2.1.1 - xinetd is installed\" : \"FAILED\" }"
else
   echo "{ \"xinetd_211_audit\" : \"PASSED\" }"
fi
