#!/bin/bash
# ** AUTO GENERATED **

# 1.10 - Ensure GDM login banner is configured (Scored)

if [ $(rpm -qa | grep gdm | wc -l) -ne 0 ]; then
 
echo "{ \"1.10 - GDM is not installed\" : \"FAILED\" }"
      
else
 echo "{ \"gdm_110_audit\" : \"PASSED\" }"
fi
