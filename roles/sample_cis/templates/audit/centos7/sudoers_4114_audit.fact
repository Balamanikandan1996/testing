#!/bin/bash
# ** AUTO GENERATED **

#  4.1.14 - Ensure changes to system administration scope (sudoers) is collected

if [ $(sudo grep scope /etc/audit/rules.d/*.rules 2> /dev/null | wc -l) -eq 0 ] || [ $(sudo auditctl -l | grep scope | wc -l) -eq 0 ]; then
   echo "{\"4.1.14 - Ensure changes to system administration scope (sudoers) is collected\":\"FAILED\"}"
else
   echo "{\"sudoers_4114_audit\":\"PASSED\"}"
   exit 1
fi

